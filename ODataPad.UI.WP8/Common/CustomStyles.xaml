<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:common="clr-namespace:ODataPad.UI.WP8.Common">

    <common:Base64ImageConverter x:Key="Base64ToImage" />

    <DataTemplate x:Key="ServiceItemTemplate">
        <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Background="Transparent"  Width="110" Height="110">
                <Image Source="{Binding ImageBase64, Converter={StaticResource Base64ToImage}}" Stretch="UniformToFill"/>
            </Border>
            <StackPanel Grid.Column="1" VerticalAlignment="Top">
                <TextBlock Text="{Binding Name}" 
                                       TextWrapping="NoWrap" />
                <TextBlock Text="{Binding Url}" 
                                       TextWrapping="NoWrap" />
                <TextBlock Text="{Binding Description}" 
                                       TextWrapping="Wrap" />
            </StackPanel>
        </Grid>
    </DataTemplate>

</ResourceDictionary>
    
